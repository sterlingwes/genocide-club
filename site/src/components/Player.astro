---
import AreaGraph from './AreaGraph.astro';

interface Props {
  playDurationSeconds?: number
}

const { playDurationSeconds } = Astro.props;
---

<AreaGraph playDurationSeconds={playDurationSeconds} />

