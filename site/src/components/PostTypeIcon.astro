---
import quote from "./icons/quote.astro";
import twitter from "./icons/twitterx.astro";

const icons = {
  quote,
  twitter,
};

interface Props {
  icon: keyof typeof icons;
}

const { icon } = Astro.props;
const Icon = icons[icon];
---

<div class="post-type-icon">
  <Icon />
</div>
