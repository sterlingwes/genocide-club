on:
  push:
    paths:
      - "**.json"
      - "**.js"
      - "**.jsx"
      - "**.mjs"
      - "**.ts"
      - "**.tsx"
      - "**.astro"
      - "**.lockb"
      - ".github/workflows/test.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - uses: actions/checkout@v3
      - uses: oven-sh/setup-bun@v1
        name: setup bun
        with:
          bun-version: 1.0.22
      - name: install dependencies
        run: bun install
      - name: stub generated data file
        run: mkdir -p site/src/generated && echo '{}' > site/src/generated/data.json
      - name: run unit tests
        run: bun run test
